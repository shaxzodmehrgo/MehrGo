<?xml version="1.0" encoding="utf-8"?><!-- Hollow route-destination ring (filled dot = pickup, ring = drop-off), driver accent colour. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="2.5dp"
        android:color="@color/app_color" />
</shape>
